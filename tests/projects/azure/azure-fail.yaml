name: test-failing-prereqs
metadata:
  template: test-project@0.0.1

reqs:
  # This will pass - Node.js is installed
  - id: nodejs
    minVersion: "20.0.0"
    
  # This might fail - not many people have go installed
  - id: go
    minVersion: "1.20.0"
    
  # This will likely fail - very high version requirement
  - id: python
    minVersion: "99.0.0"
    
  # This should pass
  - id: azd
    minVersion: "1.0.0"
