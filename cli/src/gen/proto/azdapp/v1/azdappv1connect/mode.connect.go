// ModeService toggles the dashboard's primary log source between local
// process logs and Azure Log Analytics.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: azdapp/v1/mode.proto

package azdappv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jongio/azd-app/cli/src/gen/proto/azdapp/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ModeServiceName is the fully-qualified name of the ModeService service.
	ModeServiceName = "azdapp.v1.ModeService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ModeServiceGetModeProcedure is the fully-qualified name of the ModeService's GetMode RPC.
	ModeServiceGetModeProcedure = "/azdapp.v1.ModeService/GetMode"
	// ModeServiceSetModeProcedure is the fully-qualified name of the ModeService's SetMode RPC.
	ModeServiceSetModeProcedure = "/azdapp.v1.ModeService/SetMode"
)

// ModeServiceClient is a client for the azdapp.v1.ModeService service.
type ModeServiceClient interface {
	// GetMode returns the active LogMode for this project. Replaces
	// GET /api/mode.
	GetMode(context.Context, *connect.Request[v1.GetModeRequest]) (*connect.Response[v1.GetModeResponse], error)
	// SetMode switches the active LogMode. Replaces PUT /api/mode.
	SetMode(context.Context, *connect.Request[v1.SetModeRequest]) (*connect.Response[v1.SetModeResponse], error)
}

// NewModeServiceClient constructs a client for the azdapp.v1.ModeService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewModeServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ModeServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	modeServiceMethods := v1.File_azdapp_v1_mode_proto.Services().ByName("ModeService").Methods()
	return &modeServiceClient{
		getMode: connect.NewClient[v1.GetModeRequest, v1.GetModeResponse](
			httpClient,
			baseURL+ModeServiceGetModeProcedure,
			connect.WithSchema(modeServiceMethods.ByName("GetMode")),
			connect.WithClientOptions(opts...),
		),
		setMode: connect.NewClient[v1.SetModeRequest, v1.SetModeResponse](
			httpClient,
			baseURL+ModeServiceSetModeProcedure,
			connect.WithSchema(modeServiceMethods.ByName("SetMode")),
			connect.WithClientOptions(opts...),
		),
	}
}

// modeServiceClient implements ModeServiceClient.
type modeServiceClient struct {
	getMode *connect.Client[v1.GetModeRequest, v1.GetModeResponse]
	setMode *connect.Client[v1.SetModeRequest, v1.SetModeResponse]
}

// GetMode calls azdapp.v1.ModeService.GetMode.
func (c *modeServiceClient) GetMode(ctx context.Context, req *connect.Request[v1.GetModeRequest]) (*connect.Response[v1.GetModeResponse], error) {
	return c.getMode.CallUnary(ctx, req)
}

// SetMode calls azdapp.v1.ModeService.SetMode.
func (c *modeServiceClient) SetMode(ctx context.Context, req *connect.Request[v1.SetModeRequest]) (*connect.Response[v1.SetModeResponse], error) {
	return c.setMode.CallUnary(ctx, req)
}

// ModeServiceHandler is an implementation of the azdapp.v1.ModeService service.
type ModeServiceHandler interface {
	// GetMode returns the active LogMode for this project. Replaces
	// GET /api/mode.
	GetMode(context.Context, *connect.Request[v1.GetModeRequest]) (*connect.Response[v1.GetModeResponse], error)
	// SetMode switches the active LogMode. Replaces PUT /api/mode.
	SetMode(context.Context, *connect.Request[v1.SetModeRequest]) (*connect.Response[v1.SetModeResponse], error)
}

// NewModeServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewModeServiceHandler(svc ModeServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	modeServiceMethods := v1.File_azdapp_v1_mode_proto.Services().ByName("ModeService").Methods()
	modeServiceGetModeHandler := connect.NewUnaryHandler(
		ModeServiceGetModeProcedure,
		svc.GetMode,
		connect.WithSchema(modeServiceMethods.ByName("GetMode")),
		connect.WithHandlerOptions(opts...),
	)
	modeServiceSetModeHandler := connect.NewUnaryHandler(
		ModeServiceSetModeProcedure,
		svc.SetMode,
		connect.WithSchema(modeServiceMethods.ByName("SetMode")),
		connect.WithHandlerOptions(opts...),
	)
	return "/azdapp.v1.ModeService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ModeServiceGetModeProcedure:
			modeServiceGetModeHandler.ServeHTTP(w, r)
		case ModeServiceSetModeProcedure:
			modeServiceSetModeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedModeServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedModeServiceHandler struct{}

func (UnimplementedModeServiceHandler) GetMode(context.Context, *connect.Request[v1.GetModeRequest]) (*connect.Response[v1.GetModeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("azdapp.v1.ModeService.GetMode is not implemented"))
}

func (UnimplementedModeServiceHandler) SetMode(context.Context, *connect.Request[v1.SetModeRequest]) (*connect.Response[v1.SetModeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("azdapp.v1.ModeService.SetMode is not implemented"))
}
