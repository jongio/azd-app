// LifecycleService covers transport-level liveness, environment detection,
// and the broadcast fanout channel that today is /api/ws.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: azdapp/v1/lifecycle.proto

package azdappv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jongio/azd-app/cli/src/gen/proto/azdapp/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// LifecycleServiceName is the fully-qualified name of the LifecycleService service.
	LifecycleServiceName = "azdapp.v1.LifecycleService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// LifecycleServicePingProcedure is the fully-qualified name of the LifecycleService's Ping RPC.
	LifecycleServicePingProcedure = "/azdapp.v1.LifecycleService/Ping"
	// LifecycleServiceGetEnvironmentProcedure is the fully-qualified name of the LifecycleService's
	// GetEnvironment RPC.
	LifecycleServiceGetEnvironmentProcedure = "/azdapp.v1.LifecycleService/GetEnvironment"
	// LifecycleServiceStreamBroadcastProcedure is the fully-qualified name of the LifecycleService's
	// StreamBroadcast RPC.
	LifecycleServiceStreamBroadcastProcedure = "/azdapp.v1.LifecycleService/StreamBroadcast"
)

// LifecycleServiceClient is a client for the azdapp.v1.LifecycleService service.
type LifecycleServiceClient interface {
	// Ping is a liveness check. Cheap, no side effects, no auth required.
	// Replaces GET /api/ping.
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
	// GetEnvironment returns Codespace + AZD environment metadata used by the
	// dashboard to decide whether to rewrite localhost URLs and which Azure
	// environment to read. Replaces GET /api/environment.
	GetEnvironment(context.Context, *connect.Request[v1.GetEnvironmentRequest]) (*connect.Response[v1.GetEnvironmentResponse], error)
	// StreamBroadcast is the fanout channel currently exposed as the /api/ws
	// WebSocket. The server emits coarse-grained UI hints (service-list-changed,
	// log-config-saved, mode-toggled, ...) so the dashboard can invalidate
	// caches without polling.
	//
	// BACK-PRESSURE POLICY: drop-oldest with slow-consumer disconnect.
	// Rationale: broadcast events are best-effort UI hints. A slow client must
	// not block the producer or starve other subscribers. The existing
	// websocket implementation already disconnects clients that fall too far
	// behind (see broadcast_test.go, websocket_concurrency_test.go); we keep
	// the same semantics. Clients must be resilient to dropped events and
	// re-fetch authoritative state on reconnect.
	StreamBroadcast(context.Context, *connect.Request[v1.StreamBroadcastRequest]) (*connect.ServerStreamForClient[v1.StreamBroadcastResponse], error)
}

// NewLifecycleServiceClient constructs a client for the azdapp.v1.LifecycleService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLifecycleServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LifecycleServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	lifecycleServiceMethods := v1.File_azdapp_v1_lifecycle_proto.Services().ByName("LifecycleService").Methods()
	return &lifecycleServiceClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+LifecycleServicePingProcedure,
			connect.WithSchema(lifecycleServiceMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
		getEnvironment: connect.NewClient[v1.GetEnvironmentRequest, v1.GetEnvironmentResponse](
			httpClient,
			baseURL+LifecycleServiceGetEnvironmentProcedure,
			connect.WithSchema(lifecycleServiceMethods.ByName("GetEnvironment")),
			connect.WithClientOptions(opts...),
		),
		streamBroadcast: connect.NewClient[v1.StreamBroadcastRequest, v1.StreamBroadcastResponse](
			httpClient,
			baseURL+LifecycleServiceStreamBroadcastProcedure,
			connect.WithSchema(lifecycleServiceMethods.ByName("StreamBroadcast")),
			connect.WithClientOptions(opts...),
		),
	}
}

// lifecycleServiceClient implements LifecycleServiceClient.
type lifecycleServiceClient struct {
	ping            *connect.Client[v1.PingRequest, v1.PingResponse]
	getEnvironment  *connect.Client[v1.GetEnvironmentRequest, v1.GetEnvironmentResponse]
	streamBroadcast *connect.Client[v1.StreamBroadcastRequest, v1.StreamBroadcastResponse]
}

// Ping calls azdapp.v1.LifecycleService.Ping.
func (c *lifecycleServiceClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// GetEnvironment calls azdapp.v1.LifecycleService.GetEnvironment.
func (c *lifecycleServiceClient) GetEnvironment(ctx context.Context, req *connect.Request[v1.GetEnvironmentRequest]) (*connect.Response[v1.GetEnvironmentResponse], error) {
	return c.getEnvironment.CallUnary(ctx, req)
}

// StreamBroadcast calls azdapp.v1.LifecycleService.StreamBroadcast.
func (c *lifecycleServiceClient) StreamBroadcast(ctx context.Context, req *connect.Request[v1.StreamBroadcastRequest]) (*connect.ServerStreamForClient[v1.StreamBroadcastResponse], error) {
	return c.streamBroadcast.CallServerStream(ctx, req)
}

// LifecycleServiceHandler is an implementation of the azdapp.v1.LifecycleService service.
type LifecycleServiceHandler interface {
	// Ping is a liveness check. Cheap, no side effects, no auth required.
	// Replaces GET /api/ping.
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
	// GetEnvironment returns Codespace + AZD environment metadata used by the
	// dashboard to decide whether to rewrite localhost URLs and which Azure
	// environment to read. Replaces GET /api/environment.
	GetEnvironment(context.Context, *connect.Request[v1.GetEnvironmentRequest]) (*connect.Response[v1.GetEnvironmentResponse], error)
	// StreamBroadcast is the fanout channel currently exposed as the /api/ws
	// WebSocket. The server emits coarse-grained UI hints (service-list-changed,
	// log-config-saved, mode-toggled, ...) so the dashboard can invalidate
	// caches without polling.
	//
	// BACK-PRESSURE POLICY: drop-oldest with slow-consumer disconnect.
	// Rationale: broadcast events are best-effort UI hints. A slow client must
	// not block the producer or starve other subscribers. The existing
	// websocket implementation already disconnects clients that fall too far
	// behind (see broadcast_test.go, websocket_concurrency_test.go); we keep
	// the same semantics. Clients must be resilient to dropped events and
	// re-fetch authoritative state on reconnect.
	StreamBroadcast(context.Context, *connect.Request[v1.StreamBroadcastRequest], *connect.ServerStream[v1.StreamBroadcastResponse]) error
}

// NewLifecycleServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLifecycleServiceHandler(svc LifecycleServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	lifecycleServiceMethods := v1.File_azdapp_v1_lifecycle_proto.Services().ByName("LifecycleService").Methods()
	lifecycleServicePingHandler := connect.NewUnaryHandler(
		LifecycleServicePingProcedure,
		svc.Ping,
		connect.WithSchema(lifecycleServiceMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	lifecycleServiceGetEnvironmentHandler := connect.NewUnaryHandler(
		LifecycleServiceGetEnvironmentProcedure,
		svc.GetEnvironment,
		connect.WithSchema(lifecycleServiceMethods.ByName("GetEnvironment")),
		connect.WithHandlerOptions(opts...),
	)
	lifecycleServiceStreamBroadcastHandler := connect.NewServerStreamHandler(
		LifecycleServiceStreamBroadcastProcedure,
		svc.StreamBroadcast,
		connect.WithSchema(lifecycleServiceMethods.ByName("StreamBroadcast")),
		connect.WithHandlerOptions(opts...),
	)
	return "/azdapp.v1.LifecycleService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LifecycleServicePingProcedure:
			lifecycleServicePingHandler.ServeHTTP(w, r)
		case LifecycleServiceGetEnvironmentProcedure:
			lifecycleServiceGetEnvironmentHandler.ServeHTTP(w, r)
		case LifecycleServiceStreamBroadcastProcedure:
			lifecycleServiceStreamBroadcastHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLifecycleServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLifecycleServiceHandler struct{}

func (UnimplementedLifecycleServiceHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("azdapp.v1.LifecycleService.Ping is not implemented"))
}

func (UnimplementedLifecycleServiceHandler) GetEnvironment(context.Context, *connect.Request[v1.GetEnvironmentRequest]) (*connect.Response[v1.GetEnvironmentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("azdapp.v1.LifecycleService.GetEnvironment is not implemented"))
}

func (UnimplementedLifecycleServiceHandler) StreamBroadcast(context.Context, *connect.Request[v1.StreamBroadcastRequest], *connect.ServerStream[v1.StreamBroadcastResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("azdapp.v1.LifecycleService.StreamBroadcast is not implemented"))
}
