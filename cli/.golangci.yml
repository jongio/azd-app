version: 2

run:
  timeout: 5m
  tests: true
  go: "1.25"
  build-tags:
    - integration

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
