{
  "name": "azd-app-evals",
  "private": true,
  "description": "Vally evaluation suites for azd-app copilot skills",
  "scripts": {
    "eval": "vally eval",
    "eval:smoke": "vally eval --suite smoke",
    "eval:pr": "vally eval --suite pr",
    "eval:routing": "vally eval --suite routing",
    "eval:integration": "vally eval --suite integration",
    "eval:full": "vally eval --suite full",
    "eval:skill": "vally eval --eval-spec",
    "grade": "vally grade --eval-spec"
  },
  "devDependencies": {
    "@microsoft/vally-cli": "^0.12.0"
  }
}
