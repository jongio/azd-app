# Test project for azd app reqs --generate
# This file tests the inline empty array edge case

name: reqs-generate-test

# This inline empty array should be properly handled
reqs:
  - name: node
    minVersion: "22.0.0"
  - name: pnpm
    minVersion: "9.0.0"
  - name: azd
    minVersion: "1.22.0"

services:
  web:
    project: .
    host: staticwebapp
    language: js
